ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`{{ traefik_dashboard_host }}`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All
